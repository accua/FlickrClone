<h1>All Pictures</h1>

<ul>
  <% @pictures.each do |picture| %>
    <li>  <%= picture.title %>  </li>
    
    <%= image_tag(picture.local_pic.url(:full)) %>
    <% end %>
</ul>

<%= link_to "Add a Picture", new_picture_path %>
